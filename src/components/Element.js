import React from "react";
import styled from "styled-components";

const ElementWrapper = styled.div`
	background-color: purple;
	color: black;
`;

const Element = () => {
	return <ElementWrapper>Test</ElementWrapper>;
};

export default Element;
